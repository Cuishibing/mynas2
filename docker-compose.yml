version: '2'

services:
  backend:
    build: 
      context: ./mynas_service
      dockerfile: Dockerfile
    container_name: mynas-backend
    restart: always
    environment:
      - UPLOAD_DIR=/app/uploads
    volumes:
      - /home/cui/下载/temp:/app/uploads
    networks:
      - mynas-network

  frontend:
    build:
      context: ./mynas_front
      dockerfile: Dockerfile
      args:
        - API_BASE_URL=/api
    container_name: mynas-frontend
    ports:
      - "8080:8080"
    depends_on:
      - backend
    networks:
      - mynas-network
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro

networks:
  mynas-network:
    driver: bridge 